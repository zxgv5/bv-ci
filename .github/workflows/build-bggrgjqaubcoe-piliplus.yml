name: build-bggrgjqaubcoe-piliplus

on:
  workflow_dispatch:
  workflow_call:
  
env:
  GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
  
jobs:
  build:
    runs-on: ubuntu-latest
    permissions: write-all
    
    steps:
      - name: Initialization values
        run: |
          echo "BUILD_DATE=$(TZ=UTC-8 date +"%y.%m.%d-%H.%M.%S")" >> $GITHUB_ENV
 
      - name: Checkout MAIN source repo
        uses: actions/checkout@main
        with:
          repository: bggRGjQaUbCoE/PiliPlus
          ref: main
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@main
        with:
          java-version: "21"
          distribution: "temurin"
          cache: "gradle"
          cache-dependency-path: |
            android/*.gradle*
            android/**/gradle-wrapper.properties
            
      - name: Set up Flutter
        uses: subosito/flutter-action@main
        id: flutter-action
        with:
          channel: stable
          flutter-version-file: pubspec.yaml
          cache: true
      
      - name: Apply bottom sheet patch
        working-directory: ${{ env.FLUTTER_ROOT }}
        run: git apply $GITHUB_WORKSPACE/lib/scripts/bottom_sheet_patch.diff
        continue-on-error: true
        
      - name: Write key
        if: github.event_name == 'workflow_dispatch'
        run: |
          if [ ! -z "${{ secrets.KEYSTORE_BASE64 }}" ]; then
            echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > android/app/key.p12
            echo storeFile='key.p12' >> android/key.properties
            echo storePassword='${{ secrets.KEYSTORE_PASSWORD }}' >> android/key.properties
            echo keyAlias='${{ secrets.KEY_ALIAS }}' >> android/key.properties
            echo keyPassword='${{ secrets.KEY_ALIAS_PASSWORD }}' >> android/key.properties
          fi
          
      - name: Set and Extract version
        shell: pwsh
        run: lib/scripts/build.ps1 android
        
      - name: Flutter build apk
        # run: flutter build apk --release --split-per-abi --dart-define-from-file=pili_release.json --pub
        # 改为仅编译arm64-v8a架构版本
        run: flutter build apk --release --target-platform=android-arm64 --dart-define-from-file=pili_release.json --pub
        
      - name: Rename apk
        run: |
          # for file in build/app/outputs/flutter-apk/app-*-release.apk; do
            # abi=$(echo "$file" | sed -E 's|.*app-(.*)-release\.apk|\1|')
            # mv "$file" "PiliPlus_android_${{ env.version }}_${abi}.apk"
          # done
          # 配合仅编译arm64-v8a架构版本导致的文件名变化进行的修改
          mv "app-release.apk" "PiliPlus_android_${{ env.version }}_${abi}.apk"
        shell: bash
        
      - name: Create GitHub Release & Upload APK
        uses: softprops/action-gh-release@master
        with:
          tag_name: ${{env.BUILD_DATE}}-piliplus
          files: |
              PiliPlus_android_*-v8a.apk
          draft: false
          prerelease: false